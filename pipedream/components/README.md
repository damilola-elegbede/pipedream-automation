# Pipedream Components

This directory contains reusable Pipedream components that can be shared across workflows.

## Structure

- `actions/` - Reusable action components
- `sources/` - Event source components  
- `common/` - Shared utilities and helpers

## Usage

Components in this directory can be imported and used in multiple workflows, promoting code reuse and maintainability.